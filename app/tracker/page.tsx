'use client'
import{useEffect,useState}from'react'
export default function Tracker(){const[jobs,setJobs]=useState<any[]>([]);useEffect(()=>{fetch('/api/jobs/save').then(r=>r.json()).then(d=>setJobs(d.jobs||[]))},[]);return <main className="container"><div className="nav"><div className="brand">Job Tracker</div><div className="navlinks"><a href="/">Analyze</a><a href="/resumes">Resumes</a></div></div><div className="card"><h1>Jobs</h1><table className="table"><thead><tr><th>Company</th><th>Role</th><th>Score</th><th>Resume</th><th>Status</th><th>Discovered</th></tr></thead><tbody>{jobs.map(j=><tr key={j.id}><td>{j.company}</td><td>{j.job_title}</td><td>{j.match_score??''}</td><td>{j.recommended_resume||''}</td><td>{j.status}</td><td>{j.discovered_at?.slice(0,10)}</td></tr>)}</tbody></table></div></main>}
